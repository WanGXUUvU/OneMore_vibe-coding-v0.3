# Internal

This directory is for local-only generated outputs and local misc files.

Tracked source files should live outside `_internal/`.

Ignored local paths:

- `agent-skills/`
- `claude-local/`
